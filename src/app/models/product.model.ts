export interface Product {
  id: number;
  name: string;
  description: string;
  price: number;
  image_Url: string;
  category: string;
  stock: number;
  is_active: boolean;
  created_at: string;
}
